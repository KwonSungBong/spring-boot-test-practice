server:
  port: 8888

spring:
  jpa:
    hibernate:
      ddl-auto: create
    generate-ddl: false
    show-sql: false
    database-platform: org.hibernate.dialect.MySQL5Dialect

  datasource:
    url: jdbc:mysql://localhost:3308/test?characterEncoding=utf8&useUnicode=true&autoReconnect=true&useSSL=false&zeroDateTimeBehavior=convertToNull
    username: root
    password: password
    driver-class-name: com.mysql.jdbc.Driver

  redis:
    host: localhost
    password:
    port: 7777

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
