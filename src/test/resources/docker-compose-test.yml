version: '3.3'

services:
  test_redis:
    image: redis:4
  test_mysql:
    image: mysql:5
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_USER: testtest
      MYSQL_PASSWORD: testtest
      MYSQL_DATABASE: testtest
