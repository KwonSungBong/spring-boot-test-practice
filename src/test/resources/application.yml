server:
  port: 8888

spring:
  datasource:
    url: jdbc:h2:mem:TEST;MVCC=true;DB_CLOSE_DELAY=-1;MODE=Mysql
    username: sa
    password:
    driver-class-name: org.h2.Driver
    platform: h2

  h2:
    console:
      enabled: true
      path: /h2-console

  redis:
    host: localhost
    password:
    port: 7777

  rabbitmq:
    host: localhost
    port: 6666
    username: guest
    password: guest
